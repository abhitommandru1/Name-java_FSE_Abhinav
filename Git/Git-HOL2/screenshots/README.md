# Screenshot Placeholders - Git-HOL2

Place the following screenshots in this folder when documenting the hands-on:
- `git status` before staging
- `git add` and staged state
- `git commit` output
- `git log` history view
